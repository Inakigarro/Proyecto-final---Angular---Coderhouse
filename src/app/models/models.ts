export interface Alumno {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
}

export interface Profesor {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
}
