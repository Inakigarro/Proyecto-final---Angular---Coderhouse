export interface Alumno {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
}

export interface Profesor {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
}
